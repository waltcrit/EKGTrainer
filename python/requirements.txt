# HTTP server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# ECG image digitization
# ECG-Digitiser (PhysioNet 2024 Challenge winner)
# Install from GitHub — not yet on PyPI
# In Dockerfile: RUN pip install git+https://github.com/felixkrones/ECG-Digitiser.git
# Listed here for documentation; the Dockerfile handles the actual install.
# ecg-digitiser

# Fallback digitizer dependency (used if ecg-digitiser not available)
opencv-python-headless>=4.8.0

# Signal processing
biosppy>=0.8.0
peakutils>=1.3.0
numpy>=1.24.0
scipy>=1.11.0

# Image handling
Pillow>=10.0.0

# ── Arrhythmia pipeline (python/arrhythmia/) ──────────────────────────────

# WFDB record ingestion (PhysioNet-compatible format)
# Enables load_record() in arrhythmia/ingest.py
wfdb>=4.1.0

# Wavelet features in arrhythmia/features.py
# Optional — pipeline degrades gracefully without it
PyWavelets>=1.4.0

# Machine-learning utilities used by arrhythmia/scoring.py
scikit-learn>=1.3.0

# Deep learning models (arrhythmia/models/)
# Optional — classical pipeline runs without PyTorch
# torch>=2.1.0
