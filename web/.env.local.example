# Copy to .env.local and adjust for local development.

# Python ECG pipeline (optional — defaults to http://localhost:8000)
# PYTHON_SERVICE_URL=http://localhost:8000

# Match PYTHON_API_KEY on the Python service when auth is enabled there.
# PYTHON_API_KEY=your_random_long_secret
